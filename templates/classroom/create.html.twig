<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Document</title>
</head>
<body>
    <center>
        {% if (isUpdate) %}
            <h1> Modifier classe </h1>
        {% else %}
            <h1> Créer une nouvelle classe </h1>
        {% endif %}

        {{ form_start(formClass) }}
            {{ form_widget(formClass.Name ) }} <br>
            {% if (isUpdate) %}
                {{ form_widget(formClass.Ajouter , { label : 'modifier' }) }}
            {% else %}
                {{ form_widget(formClass.Ajouter) }}
            {% endif %}
        {{ form_end(formClass) }}

    </center>
</body>
</html>